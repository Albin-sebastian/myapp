import {
    styled,
    Card,
    CardActionArea,
    CardActions,
    CardContent,
    CardMedia
} from "@mui/material";

export const CnCard = styled(Card)({

});
export const CnCardActionArea = styled(CardActionArea)({


});
export const CnCardActions = styled(CardActions)({

});
export const CnCardContent = styled(CardContent)({

});
export const CnCardMedia = styled(CardMedia)({

});
